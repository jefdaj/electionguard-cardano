use pubsub/tests as t

// use cardano/transaction.{Transaction, placeholder}
// use pubsub
// use pubsub/types.{PubsubDatum, PubsubAction, PsClose, PsFund, PsOpen, PsPublish}

test pubsubOpen() {
  // let tx = Transaction {
  //   .. placeholder,
  // }
  // pubsub.pubsub.mint(PsOpen, _policyId, _tx)
  True
}
