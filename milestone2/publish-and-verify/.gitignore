# Cardano preview wallet keys with tADA from the faucet
offchain/*keys/

# Every time we parameterize the contract, we save the resulting JSON just in
# case we need it to recover some assets later. Normally they get bulk deleted
# after a couple days.
onchain/election-plutus-*.json

# Every time we publish a new election on testnet, we save it as JSON too.
offchain/election-*.json

*.log
